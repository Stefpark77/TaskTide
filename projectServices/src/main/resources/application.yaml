spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3307/tasktide
    username: root
    password: Stefpark77$
server:
  servlet:
    context-path: /projectServices
  port: 8081
  #port: 9400 # uncomment for integration Tests

security:
  jwt:
    secret-key: test_secret_key